#include <math.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

#include <functions.h>

/**
 * @brief 2520 is the smallest number that can be divided by each of the
 * numbers from 1 to 10 without any remainder.
 *
 * What is the smallest positive number that is evenly divisible by all of the
 * numbers from 1 to 20?
 */
int main(void)
{
    printf("Largest Palindrome is not_needed\n");
    return 0;
}